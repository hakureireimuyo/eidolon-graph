"""节点封装层:内核引用外部能力库(独立子项目),把能力包装成节点。

- 能力库(如 eidolon-llm)完全独立:零图概念、零协议依赖,任何上层可用;
- 本层把能力包装成节点类型与执行逻辑(节点协议 ABI 的唯一实现面);
- 内置原语(engine/builtins)仍属内核级构造材料,与封装层并列。

登记入口由各子层提供(如 register_llm_nodes),宿主显式调用(协议 §6)。
"""
